{
    "name": "pd-mcp",
    "private": true,
    "description": "Price Discussion's public MCP server — one zero-dependency Cloudflare Worker.",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "check": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250705.0",
        "typescript": "^5.5.0",
        "wrangler": "^4.0.0"
    }
}
