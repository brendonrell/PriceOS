# Step 2.1 — bug fixes

Two replacement files. Drag both into the upload area on `dev`,
commit, wait for the green check, screenshot.

| File | Path |
|---|---|
| globals.css | `app/globals.css` |
| UserMenuButtons.tsx | `components/shell/UserMenuButtons.tsx` |

Commit message: `step 2.1: bubble + sprite visibility fixes`

After this commits and looks right on the dev preview, the next
thing to do is merge the open Pull Request to clean up main.
I'll walk you through that part once the dev preview looks good.
